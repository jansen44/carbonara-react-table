import { CarbonaraGroupedDataDefinition } from '../misc'

export type CarbonaraTBodyLoadingProps = {
  columns: CarbonaraGroupedDataDefinition[],
  rowHeight?: number,
  shimmerLoadingRowsTotal: number
}
